info:
  version: ${project.version}

spring:
  application:
    name: iov-console-web
  http:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
  thymeleaf:
    mode: HTML5
    prefix: classpath:/templates
    encoding: UTF-8
    content-type: text/html
    cache: false
    cache-period: 0
  template:
    cache: false
  cloud:
    config:
      username: tima
      password: FRkjGiRMqem79GHE
      discovery:
        enabled: true
        serviceId: CONFIG-SERVER
      profile: tpc-poc
      label: develop

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://172.20.38.96:8761/eureka/
  instance:
    leaseRenewalIntervalInSeconds: 10
    statusPageUrlPath: ${management.context-path}/info
    healthCheckUrlPath: ${management.context-path}/health
    metadataMap:
      instanceId: ${vcap.application.instance_id:${spring.application.name}:${spring.application.instance_id:${server.port}}}
